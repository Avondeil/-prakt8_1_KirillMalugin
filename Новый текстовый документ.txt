import java.text.DateFormat
import java.util.*

fun main() {
    try {
        print("Введите название газеты: ")
        var name = readLine()!!.toString()
        if(name>="a" && name<="z" || name>="а" && name <="я" &&name>="A" && name<="Z" || name>="А" && name <="я" )
        {
        print("Введите номер газеты: ")
        var number = readLine()!!.toInt()
        if (number>0) {
            print("Введите дату выпуска газеты: ")
            var date = readLine()!!.toString()
            val news1: Newspapers = Newspapers(name, number,date )
            println("Название газеты: ${news1.name}")
            println("Номер газеты: ${news1.number}")
            println("Дата выпуска газеты: ${news1.date}")
        } else println("Номер не может быть отрицательным")
        }else print("Ввод числа и знаков в названии запрещен!")
    } catch (e:Exception)
    {
        println("Найдена ошибка при вводе данных")
    }
}